compile:
  override:
    - mkdir build
    - cd build && cmake -DBUILD_FORTRAN=ON -DBUILD_PACKAGE=ON -DCPACK_GENERATOR=STGZ .. && make -j 4 package
test:
  override:
    - build/Products/energyplus_tests
